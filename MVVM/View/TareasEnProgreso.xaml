<Page x:Class="PracticaEx.MVVM.View.TareasEnProgreso"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PracticaEx.MVVM.View"
      mc:Ignorable="d"
      DataContext="{StaticResource AppViewModel}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TareasEnProgreso">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ListBox x:Name="ListaTareas" ItemsSource="{Binding ListaTareasEnProgreso}" SelectedItem="{Binding TareaSeleccionada}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="10">
                            <StackPanel Orientation="Horizontal" Margin="5" VerticalAlignment="Center">
                                <TextBlock Text="Nombre:" Margin="0,0,5,0" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Nombre}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <TextBlock Text="Estado:" Margin="0,0,5,0" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Estado}"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</Page>
